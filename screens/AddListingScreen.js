import { View, Text } from 'react-native';

export default function AddListingScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Add Listing Form (Coming Next)</Text>
    </View>
  );
}
